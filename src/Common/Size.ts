/**
* name 
*/
module smallGame{
	export class Size{

		public width: number;
		public height: number;

		constructor(mWidth:number, mHeight:number){
			this.width = mWidth;
			this.height = mHeight;
		}
	}
}